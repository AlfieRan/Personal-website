import { Center, Text } from "@chakra-ui/react";

const Loading = () => {
    return (
        <Center h={"100%"} w={"100%"} textAlign={"center"}>
            <Text>Loading...</Text>
        </Center>
    );
};

export default Loading;
